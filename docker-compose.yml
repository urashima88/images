networks:
  pixora:
    external: true

services:
  images_app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - ${APP_PORT}:${APP_PORT}
    environment:
      - DB_HOST=images-postgres-db # container_name of db
    networks:
      - pixora
    env_file:
      - .env
    container_name: images-app

  images_postgres_db:
    image: postgres
    restart: always
    ports:
      - ${DB_PORT}:${DB_PORT}
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_SSL_MODE=${SSL_MODE}
    networks:
      - pixora
    volumes:
      - /var/lib/postgresql/18/docker
    env_file:
      - .env
    container_name: images-postgres-db
    
